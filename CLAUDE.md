# CLAUDE.md

이 저장소는 Hello, NPU 랩의 전자책 "리벨리온 NPU 실전 가이드" 원본입니다. 위키독스와 연동되어 `main` push 시 자동 발행됩니다.

## 먼저 읽을 것

작성 규약은 [AGENTS.md](AGENTS.md)를 따릅니다. (파일명·TOC·스타일·합니다체·용어집·"지어내지 않기" 규칙)

## 자동화 슬래시 명령

13명의 글을 일관되게 유지하기 위한 명령입니다. (`.claude/commands/`)

- `/draft <발표자료나 메모>` — 자료를 받아 [템플릿](templates/chapter.md)과 AGENTS 규약에 맞는 챕터 초안을 `pages/`에 생성
- `/normalize <파일들>` — 용어집 기준 표기 통일 + 합니다체 교정 + 제목·코드블록·링크 규칙 점검
- `/toc` — `pages/`의 H1을 읽어 `TOC.md`를 재생성 (번호순, 제목 일치)
- `/publish` — 변경된 페이지를 커밋하고 `main`에 push (위키독스 자동 동기화). 무엇이 발행되는지 먼저 요약

## 작업 원칙

- 한 번에 한 장씩. 사실 확인이 필요한 SDK 명령·수치는 지어내지 말고 `> 📝 실습 채움`으로 남깁니다.
- 페이지를 추가·이동하면 반드시 `TOC.md`를 갱신합니다 (`/toc`).
- 원본은 이 레포 본체뿐입니다. 위키독스/다른 곳을 직접 고치지 않습니다.
