# AGENTS.md

사람과 AI가 이 저장소에서 글을 쓸 때 따르는 규약입니다. 13명이 함께 쓰는 책의 일관성을 위해, 여기 규칙을 지켜주세요.

## 저장소 성격

[위키독스](https://wikidocs.net)와 연동된 문서 저장소입니다. `main`에 push하면 위키독스에 자동 반영됩니다. 빌드/테스트 과정은 없습니다.

```
TOC.md      # 목차 (페이지 구조 정의)
pages/      # 본문 (*.md)
assets/     # 이미지
templates/  # 작성 템플릿 (발행 안 됨)
```

## 파일 이름 규칙 (pages/)

- 번호 접두사 + kebab-case: `01-npu-intro.md`, `09-llm-serving.md`
- 위키독스는 파일명을 알파벳/숫자순으로 정렬하므로 **번호 접두사는 두 자리**(`01`, `02` … `12`)로 맞춥니다.
- 한 페이지에 H1(`#`)은 하나, **H1 제목의 번호와 파일 번호를 일치**시킵니다. 예: `01-npu-intro.md` → `# 01. NPU란 무엇인가 / GPU vs NPU`

## TOC.md 규칙

- 모든 페이지는 반드시 `TOC.md`에 등록합니다. (없으면 위키독스에 안 보임)
- 목록 기호는 `*`를 씁니다. (위키독스 요구사항)
- 하위 항목은 공백 2칸 들여쓰기.
- **TOC의 제목은 해당 페이지의 H1과 글자까지 정확히 일치**해야 합니다.

## 마크다운 스타일

- 본문 목록은 `-`, 순서 목록은 `1.` (들여쓰기 2칸). 단 TOC만 `*`.
- 제목 단계를 건너뛰지 않습니다. (`##` 다음 `####` 금지)
- 코드 블록에는 언어를 명시합니다. (```python 등)
- 이미지는 `assets/`에 두고 상대경로로 참조: `![설명](../assets/example.png)`
- 페이지 간 링크는 같은 `pages/` 안의 상대경로: `[08. vLLM 이해하기](08-vllm-basics.md)`

## 글쓰기

- 언어는 한국어, **합니다체**(정중체).
- 기술 용어 표기는 [용어집](pages/13-glossary.md)을 따릅니다. 처음 등장 시 영문 병기.
- 챕터 구조는 [템플릿](templates/chapter.md)을 따릅니다: 개요 / 배경·이론 / 실습 / 결과 / 막힌 점 / 참고.
- **추정값·지어낸 수치를 쓰지 않습니다.** 실제 측정·실행 결과만 적고, 아직 없으면 `> 📝 실습 채움` 자리표시로 남깁니다.

## 새 페이지 추가 절차

1. `pages/NN-slug.md` 생성 (H1 번호 일치)
2. `TOC.md`의 올바른 위치에 항목 추가
3. 커밋 후 PR → 리뷰 → 머지

## 커밋 메시지

Conventional Commits를 사용합니다.

```
docs: 08장 LLM 서빙 실습 결과 추가
fix: 깨진 이미지 링크 수정
chore: assets 정리
```

## 중요

1. **자동 동기화**: `main` push → 위키독스 반영
2. **정렬**: 두 자리 번호 접두사로 순서 보장
3. **TOC 필수**: TOC에 없는 페이지는 발행되지 않음
